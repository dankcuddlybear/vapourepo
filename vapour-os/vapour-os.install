post_install() {
	/opt/vapour-os/vosinstall install
}

post_upgrade() {
	/opt/vapour-os/vosinstall upgrade
}
pre_remove() {
	systemctl --now disable ananicy-cpp.service avahi-daemon.service fstrim.timer irqbalance.service rtirq.service
}
post_remove() {
	rm -rf /etc/vapour-os /opt/vapour-os
}