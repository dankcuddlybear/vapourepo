pkgname=vapour-os-nvidia
pkgver=0.1
pkgrel=1
pkgdesc='Vapour OS Nvidia drivers for OpenGL, Vulkan, OpenCL and hardware video acceleration.'
arch=(any)
url='https://github.com/dankcuddlybear/Vapour-OS'
backup=(etc/X11/xorg.conf.d/20-nvidia.conf)
depends=('nvidia-dkms'
    'lib32-nvidia-utils'
    'lib32-opencl-nvidia'
    'opencl-nvidia'
)
optdepends=('cuda: CUDA runtime library and CUDA toolkit'
    'nvidia-prime: Run graphically intensive applications on the dedicated Nvidia GPU'
)