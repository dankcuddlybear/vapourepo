[Trigger]
Type = Package
Operation = Install
Operation = Upgrade
Target = breeze

[Action]
Description = Delete Plasma's ugly default cursors, forcing it to use Adwaita cursor theme instead
When = PostTransaction
Exec = sh -c 'rm -rf /usr/share/icons/breeze_cursors &> /dev/null || sleep 0'
